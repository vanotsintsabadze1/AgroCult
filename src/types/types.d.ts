export interface Blog {
  title: string;
  body: string;
  tags: string[];
  id: number;
}

export interface ShopItem {
  images: string[];
  title: string;
  description: string;
  price: number;
  id: number;
}
